import{afterEach,describe,expect,it,vi}from'vitest';import{mount}from'@vue/test-utils';import{createRouter,createMemoryHistory}from'vue-router';import ElementPlus from'element-plus';import{api}from'../api/client';import LoginPage from'../pages/LoginPage.vue';import HomePage from'../pages/HomePage.vue';import UploadPage from'../pages/UploadPage.vue';
const tick=()=>new Promise(r=>setTimeout(r,0));function testRouter(){return createRouter({history:createMemoryHistory(),routes:[{path:'/',component:{template:'<div/>'}},{path:'/login',component:{template:'<div/>'}},{path:'/upload',component:{template:'<div/>'}},{path:'/books/:id',component:{template:'<div/>'}}]})}afterEach(()=>vi.restoreAllMocks());
describe('core pages',()=>{it('submits login credentials',async()=>{const post=vi.spyOn(api,'post').mockResolvedValue({data:{}} as any);const router=testRouter();await router.push('/login');await router.isReady();const w=mount(LoginPage,{global:{plugins:[router,ElementPlus]}});const inputs=w.findAll('input');await inputs[0].setValue('admin');await inputs[1].setValue('correct horse battery staple');await w.find('form').trigger('submit');await tick();expect(post).toHaveBeenCalledWith('/auth/login',{username:'admin',password:'correct horse battery staple'})});it('renders returned books',async()=>{vi.spyOn(api,'get').mockResolvedValue({data:{items:[{id:'b1',title:'测试书籍',subtitle:'',description:'',language:'zh',publisher:'',isbn:'',cover_path:'',reading_status:'unread',rating:0,created_at:'',files:[],authors:[],tags:[]}],total:1,page:1,page_size:20}} as any);const router=testRouter();await router.push('/');await router.isReady();const w=mount(HomePage,{global:{plugins:[router,ElementPlus]}});await tick();expect(w.text()).toContain('测试书籍')});it('uploads the selected file',async()=>{const post=vi.spyOn(api,'post').mockResolvedValue({data:{book:{id:'b1'}}} as any);const router=testRouter();await router.push('/upload');await router.isReady();const w=mount(UploadPage,{global:{plugins:[router,ElementPlus]}});const input=w.find('input[type=file]');const file=new File(['%PDF-'],'book.pdf',{type:'application/pdf'});Object.defineProperty(input.element,'files',{value:[file]});await input.trigger('change');await w.find('button').trigger('click');await tick();expect(post).toHaveBeenCalled();expect((post.mock.calls[0][1]as FormData).get('file')).toBe(file)})});
